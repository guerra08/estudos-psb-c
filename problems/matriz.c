#include <stdio.h>;
#include <stdlib.h>;

int diagonal(int *mat);

int diagonal(int *mat){

    printf("%f",*mat);

}

int main() {
    float *mat = malloc(5000 * 5000 * sizeof *mat);
    

    return 0;
}
